# 量化项目模块化指南

## 1. 模块化概述

模块化是将大型项目拆分为独立、功能专一的代码块，每个模块专注于做好一件事。

## 2. 量化项目标准模块划分

```
量化项目/
│
├── 1. 数据模块 (Data)
│   ├── 数据获取          # API抓取、文件读取
│   ├── 数据清洗          # 缺失值、异常值处理
│   ├── 数据存储          # SQLite、CSV、HDF5
│   └── 数据重采样        # tick→1min→1h→1d
│
├── 2. 因子/信号模块 (Alpha)
│   ├── 技术指标          # SMA、EMA、RSI、MACD
│   ├── 趋势信号          # 双均线、VWAP
│   ├── 均值回归信号      # 布林带、KDJ
│   └── 信号组合          # 多信号加权
│
├── 3. 策略模块 (Strategy)
│   ├── 策略逻辑          # 买入/卖出规则
│   ├── 仓位管理          # 固定/动态仓位
│   ├── 止损止盈          # 固定止损、时间止损
│   └── 组合配置          # 多标的、多策略
│
├── 4. 回测模块 (Backtest)
│   ├── 订单模拟          # 成交、滑点、手续费
│   ├── 绩效计算          # 收益率、夏普、最大回撤
│   ├── 敏感性分析        # 参数扰动
│   └── 样本外测试        # train/test/holdout
│
├── 5. 执行模块 (Execution)
│   ├── 券商接口          # IB、MT4、交易所API
│   ├── 订单管理          # 下单、改单、撤单
│   └── 持仓监控          # 实时持仓同步
│
├── 6. 风控模块 (Risk)
│   ├── 仓位限制          # 单标的、总体仓位
│   ├── 风险预算          # VaR、止损线
│   └── 异常检测          # 暴跌预警
│
└── 7. 分析模块 (Analytics)
    ├── 可视化            # 资金曲线、收益分布
    ├── 归因分析          # 收益来源分解
    └── 报告生成          # 自动出报告
```

## 3. 模块间的数据流

```
数据获取 → 数据清洗 → 因子计算 → 策略信号 → 回测引擎 → 绩效分析
   ↓           ↓          ↓          ↓          ↓          ↓
  CSV      DataFrame   DataFrame   DataFrame   CSV/MD    图表/报告
```

## 4. 模块化好处

| 好处 | 说明 |
|------|------|
| **复用** | 一个模块可在多个项目用 |
| **维护** | 改一个模块不影响其他 |
| **协作** | 多人分工开发不同模块 |
| **测试** | 单独测试每个模块 |

## 5. 项目示例

### Alpha 币安项目结构

```
Alpha项目/
├── 数据/                  # 数据模块：原始CSV文件
├── 代码/
│   ├── strategy_module_experiment_v2.py  # 信号模块 + 策略模块
│   ├── backtest_experiment_v2.py         # 回测模块
│   └── stability_analysis_with_train.py  # 敏感性分析
├── 报告/                  # 分析模块：回测报告、图表
└── 实验记录/              # 文档：实验日志
```

### 外汇项目结构

```
外汇项目/
├── 数据/                  # 数据模块：原始数据
├── 数据获取/
│   ├── fetch_forex_yfinance.py   # Yahoo Finance获取
│   ├── fetch_forex_oanda.py      # OANDA API获取
│   └── fetch_forex_polygon.py    # Polygon API获取
├── 策略/
│   └── VWAP_Strategy_v1.mq4      # MT4 EA（策略模块）
├── 回测/
│   └── backtest_sma_forex.py     # SMA回测（回测模块）
└── 报告/                  # 分析模块：回测报告
```

## 6. 模块间依赖关系

```
            ┌─────────────┐
            │   数据模块    │
            └──────┬──────┘
                   │ DataFrame
                   ▼
            ┌─────────────┐
            │  信号模块    │
            └──────┬──────┘
                   │ Signal
                   ▼
            ┌─────────────┐
            │   策略模块    │
            └──────┬──────┘
                   │ Order
                   ▼
            ┌─────────────┐
            │   回测模块    │
            └──────┬──────┘
                   │ Result
                   ▼
            ┌─────────────┐
            │   分析模块    │
            └─────────────┘
```

## 7. 最佳实践

### 7.1 模块间通过函数/类交互

```python
# 正确：清晰的函数接口
def add_signals(bars: pd.DataFrame, params: dict) -> pd.DataFrame:
    """信号模块：接收清洗后的数据，返回带信号的DataFrame"""
    ...

def run_backtest(sig: pd.DataFrame, config: dict) -> tuple[pd.DataFrame, pd.DataFrame]:
    """回测模块：接收信号数据，返回结果和交易记录"""
    ...
```

### 7.2 配置文件集中管理

```python
# config.py
@dataclass
class StrategyConfig:
    vwap_fast_window: int = 20
    vwap_slow_window: int = 60
    buy_confirm_bars: int = 1
    sell_confirm_bars: int = 4

@dataclass
class RiskConfig:
    max_position_pct: float = 0.05
    max_drawdown_pct: float = 0.20
```

### 7.3 单一职责原则

每个文件/函数只做一件事：

```
# 不好：一个文件干很多事
backtest.py - 数据获取、信号计算、回测、报告全在一起

# 好：分工明确
data_loader.py   - 只负责加载数据
signals.py       - 只负责计算信号
backtest.py      - 只负责回测逻辑
report.py        - 只负责生成报告
```

## 8. 进阶：Git Submodule 跨项目复用

当一个模块需要在多个项目间共享时，使用 Git Submodule：

```bash
# 项目A：策略模块
git add strategy_module.py
git commit -m "添加策略模块"
git push

# 项目B：引用项目A的策略模块
git submodule add https://github.com/xxx/project_a.git strategy_module

# 项目B可以独立运行，同时保持对策略模块的引用
```

### Submodule 常用命令

```bash
# 添加子模块
git submodule add https://github.com/xxx/shared_repo.git path/to/module

# 克隆带子模块的项目
git clone --recurse-submodules https://github.com/xxx/main_project.git

# 更新子模块到最新
git submodule update --remote path/to/module

# 在子模块中提交更改
cd path/to/module
git checkout -b new_feature
# 修改代码...
git commit -m "更新功能"
git push

# 主项目同步子模块更新
git add path/to/module
git commit -m "同步子模块更新"
git push
```

## 9. 总结

- **模块化** = 按功能拆分代码，每个模块专注一件事
- **数据流** = 模块间通过 DataFrame/信号传递
- **好处** = 可复用、易维护、易测试
- **进阶** = Git Submodule 实现跨项目模块共享